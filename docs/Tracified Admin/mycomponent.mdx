export default function Welcome(props) {
    const count = useState(10,setCount)
  return (<div>
      {count}
    <button type="button" onclick="alert('Hello world!')">Click Me!</button>
  </div>);
}